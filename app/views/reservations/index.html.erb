<p style="color: green"><%= notice %></p>

<h1>Reservations</h1>

<!--<div id="reservations">-->
  <%# @reservations.each do |reservation| %>
    <%#= render reservation %>
<!--    <p>-->
      <%#= link_to "Show this reservation", reservation %>
<!--    </p>-->
  <%# end %>
<!--</div>-->

<table class="table table-striped table-bordered table-hover">
  <thead>
    <th scope="col" class="col-sm-2">Name</th>
    <th scope="col" class="col-sm-2">Number of People</th>
    <th scope="col" class="col-sm-2">Number of Tables</th>
    <th scope="col" class="col-sm-2">Date</th>
    <th scope="col" class="col-sm-2">Time</th>
    <th scope="col" class="col-sm-1">Edit</th>
  </thead>
  <tbody>
    <% @reservations.each do |reservation| %>
      <%= render reservation %>
    <% end %>
  </tbody>
</table>

<%= link_to "New reservation", new_reservation_path, class:"btn btn-primary"%>
